# Leave blank when your frontend host proxies /api requests to the backend.
VITE_API_BASE_URL=

# If you want the browser to call the API directly instead of using a host-level proxy,
# set this to the API origin, for example:
# VITE_API_BASE_URL=https://api.your-domain.com
